services:
  - type: web
    name: node-mesh-aegis
    env: node
    rootDir: server
    plan: free
    buildCommand: npm ci || npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
